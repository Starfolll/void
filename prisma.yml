endpoint: http://localhost:4466
secret: ${env:PRISMA_SECRET}

datamodel:
  - /db/models/datamodel.user.graphql
  - /db/models/datamodel.lobby.graphql
  - /db/models/datamodel.table.graphql
  - /db/models/datamodel.room.graphql

generate:
  - generator: typescript-client
    output: ./generated/prisma-client/
